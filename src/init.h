/* Initializes variables. */

#ifndef _INIT_H
#define _INIT_H

/* Initializes global variables. */
void initGlobalVars(int argc, char** argv);

/* Deinitializes variables. */
void deInitVars(void);

/* Initializes OpenGl*/
void initOpenGl(void);

/* Initializes lighting. */
void initLighting(void);

#endif